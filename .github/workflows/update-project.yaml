name: Issue Opened

on:
  issues:
    types: [opened]

jobs:
  move-issue:
    runs-on: ubuntu-latest
    steps:
      - uses: jason0x43/action-project-manager@master
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          project: Development
          column: To do
